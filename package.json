{
  "name": "08-weather-app-makander",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://makander.github.io/weatherapp/",
  "dependencies": {
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-icons-weather": "^1.0.5",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run remotePublish && gh-pages -d build && npm run remoteRestore",
    "remotePublish": "git remote set-url origin https://github.com/makander/weatherapp.git",
    "remoteRestore": "git remote set-url origin https://github.com/chas-academy/08-weather-app-makander"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
